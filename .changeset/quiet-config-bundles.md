---
"@weiui/css": minor
---

Add a build-time config layer for deterministic on-demand CSS bundles, real fragment subpath outputs, automatic element dependency closure, and a zero-runtime-JavaScript `weiui-css` bundle CLI.
