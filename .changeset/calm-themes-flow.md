---
"@weiui/tokens": minor
"@weiui/css": minor
---

Add a canonical `wui-theme` cascade slot for product-level semantic token overrides and make the framework-agnostic CSS consumption path explicit for non-React hosts.
