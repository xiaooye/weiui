# Runtime consumption fixtures

These intentionally tiny fixtures verify that each official runtime is consumed through its public package surface. They are not showcase apps and are kept outside the pnpm workspace to avoid multiplying framework toolchains in the main dependency graph.
