import { PackageManagerTabs } from "../../../components/docs/PackageManagerTabs";

# Getting Started

Civaria is a three-layer design system. Start with the layer that matches how you work:

- **Just CSS?** Install `@civaria/tokens` and `@civaria/css`. Zero JavaScript.
- **React, unstyled?** Add `@civaria/headless` for accessible hooks and compound components.
- **React, styled?** Use `civaria` — polished components on top of the two layers below.

## 1. Install

The quickest start uses the styled React layer. It depends on the other three packages, so installing it pulls them all in.

<PackageManagerTabs command="civaria" />

## 2. Import tokens and base CSS

Add these two lines once, at the top of your app. Tokens define the color, spacing, and typography scales; the CSS layer ships reset + component styles.

```tsx
// app/layout.tsx (or equivalent)
import "@civaria/tokens/tokens.css";
import "@civaria/css";
```

Tokens default to system light/dark via `prefers-color-scheme`. To force a theme, add `class="dark"` or `data-theme="dark"` to `<html>`.

## 3. Render your first component

```tsx
import { Button } from "civaria";

export default function Page() {
  return <Button>Hello Civaria</Button>;
}
```

Prefer a CSS-only build? Skip the import and use classes directly:

```html
<button class="civ-button civ-button--solid">Hello Civaria</button>
```

## Next steps

- [Browse components](/docs/components) — 66 components across form, overlay, data, feedback, and more.
- [Design tokens](/docs/tokens) — the OKLCH color scales, spacing, typography, motion, and shadow tokens.
- [Accessibility](/docs/accessibility) — WCAG AAA targets, keyboard patterns, and contrast guarantees.
- [CLI](/docs/cli) — `civaria init`, `add`, and `audit` for scaffolding and validation.
