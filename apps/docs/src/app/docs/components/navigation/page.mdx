import { Preview } from "../../../../components/preview/Preview";
import { TabsDemo } from "../../../../components/demos/TabsDemo";
import { LinkDemo } from "../../../../components/demos/LinkDemo";
import { BreadcrumbDemo } from "../../../../components/demos/BreadcrumbDemo";
import { MenuDemo } from "../../../../components/demos/MenuDemo";

# Navigation Components

Tabs, Menu, Breadcrumb, and Link for structuring navigation and content.

## Breadcrumb

<Preview label="Breadcrumb">
  <BreadcrumbDemo />
</Preview>

```tsx
import { Breadcrumb, BreadcrumbItem, BreadcrumbSeparator } from "@weiui/react";

<Breadcrumb>
  <BreadcrumbItem>
    <a href="/">Home</a>
  </BreadcrumbItem>
  <BreadcrumbSeparator />
  <BreadcrumbItem>
    <a href="/products">Products</a>
  </BreadcrumbItem>
  <BreadcrumbSeparator />
  <BreadcrumbItem active>
    Detail
  </BreadcrumbItem>
</Breadcrumb>
```

### Props — BreadcrumbItem

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| active | `boolean` | `false` | Marks current page; sets `aria-current="page"` |

### Accessibility

- Wrapped in `<nav aria-label="Breadcrumb">`
- Active item gets `aria-current="page"`
- Separators have `aria-hidden="true"`

---

## Tabs

Organize content into tabbed panels with keyboard navigation.

<Preview label="Tabs">
  <TabsDemo />
</Preview>

```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@weiui/react";

<Tabs defaultValue="overview">
  <TabsList>
    <TabsTrigger value="overview">Overview</TabsTrigger>
    <TabsTrigger value="features">Features</TabsTrigger>
    <TabsTrigger value="pricing">Pricing</TabsTrigger>
  </TabsList>
  <TabsContent value="overview">
    <p>Welcome to the overview tab.</p>
  </TabsContent>
  <TabsContent value="features">
    <p>Explore all available features.</p>
  </TabsContent>
  <TabsContent value="pricing">
    <p>Choose a plan that works for you.</p>
  </TabsContent>
</Tabs>
```

### Props

Props on `<Tabs>`:

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| defaultValue | `string` | — | Initially active tab (uncontrolled) |
| value | `string` | — | Active tab (controlled) |
| onValueChange | `(value: string) => void` | — | Called when active tab changes |

Props on `<TabsList>`:

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| orientation | `"horizontal" \| "vertical"` | `"horizontal"` | Arrow-key axis — `horizontal` uses Left/Right, `vertical` uses Up/Down |
| loop | `boolean` | `true` | When `true`, arrow navigation wraps at the ends |

### Keyboard

- ArrowLeft / ArrowRight move focus between tabs (ArrowUp / ArrowDown for vertical orientation)
- Home / End jump to the first / last tab
- Enter or Space activates the focused tab
- Focus wraps from last to first (disable with `loop={false}` on `<TabsList>`)

### Accessibility

- `role="tablist"`, `role="tab"`, `role="tabpanel"`
- Arrow-key roving focus — only one tab is tabbable at a time
- `aria-selected` on the active trigger
- `aria-controls` / `aria-labelledby` link triggers to panels

---

## Menu

A dropdown menu with keyboard-navigable items.

<Preview label="Menu">
  <MenuDemo />
</Preview>

```tsx
import { Menu, MenuTrigger, MenuContent, MenuItem, MenuSeparator } from "@weiui/react";

<Menu>
  <MenuTrigger className="wui-button wui-button--outline">Actions</MenuTrigger>
  <MenuContent>
    <MenuItem onSelect={() => console.log("edit")}>Edit</MenuItem>
    <MenuItem onSelect={() => console.log("duplicate")}>Duplicate</MenuItem>
    <MenuSeparator />
    <MenuItem onSelect={() => console.log("delete")}>Delete</MenuItem>
  </MenuContent>
</Menu>
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| side | `"top" \| "right" \| "bottom" \| "left"` | `"bottom"` | Preferred side of the trigger to render on |
| align | `"start" \| "center" \| "end"` | `"start"` | Alignment of the content relative to the trigger |

Menu manages its own open state internally — clicking the trigger opens it, and Escape / outside click / item selection closes it.

### Accessibility

- `role="menu"` on content, `role="menuitem"` on items
- Arrow keys navigate items
- Typeahead: pressing a letter jumps focus to the next item whose label starts with that letter
- Disabled items are skipped during keyboard navigation
- Escape closes menu
- Enter / Space selects item
- Content is rendered through a Portal and anchored with Floating UI

---

## Link

A styled anchor element with optional external link handling.

<Preview label="Link — internal + external">
  <LinkDemo />
</Preview>

```tsx
import { Link } from "@weiui/react";

{/* Internal link */}
<Link href="/docs">Documentation</Link>

{/* External link — opens in new tab with rel="noopener noreferrer" */}
<Link href="https://github.com/weiui" external>
  GitHub
</Link>
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| external | `boolean` | `false` | Adds `target="_blank"` and `rel="noopener noreferrer"` |
| href | `string` | — | Link destination |
| className | `string` | — | Additional CSS classes |

### Accessibility

- Inherits all native anchor attributes
- External links automatically set `rel="noopener noreferrer"` to prevent tab-napping
