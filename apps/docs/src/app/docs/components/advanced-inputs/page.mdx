import { Preview } from "../../../../components/preview/Preview";
import { AutoCompleteDemo } from "../../../../components/demos/AutoCompleteDemo";
import { MultiSelectDemo } from "../../../../components/demos/MultiSelectDemo";
import { FileUploadDemo } from "../../../../components/demos/FileUploadDemo";
import { SliderDemo } from "../../../../components/demos/SliderDemo";
import { RatingDemo } from "../../../../components/demos/RatingDemo";
import { InputNumberDemo } from "../../../../components/demos/InputNumberDemo";
import { InputNumberDisabledDemo } from "../../../../components/demos/InputNumberDisabledDemo";
import { InputNumberCurrencyDemo } from "../../../../components/demos/InputNumberCurrencyDemo";
import { InputOTPDemo } from "../../../../components/demos/InputOTPDemo";
import { InputOTP4Demo } from "../../../../components/demos/InputOTP4Demo";

# Advanced Inputs

Slider, Rating, InputNumber, and InputOTP — specialized input controls for ranges, ratings, numeric values, and one-time passwords.

---

## Slider

A range slider with min/max/step support and full keyboard navigation.

<Preview label="Slider">
  <SliderDemo />
</Preview>

```tsx
import { Slider } from "@weiui/react";

<Slider defaultValue={40} />
<Slider min={0} max={200} step={10} defaultValue={100} onChange={(v) => console.log(v)} />
<Slider disabled defaultValue={60} />
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| min | `number` | `0` | Minimum value |
| max | `number` | `100` | Maximum value |
| step | `number` | `1` | Step increment |
| value | `number` | — | Controlled value |
| defaultValue | `number` | `0` | Initial uncontrolled value |
| onChange | `(value: number) => void` | — | Called on value change |
| disabled | `boolean` | `false` | Disables the slider |
| label | `string` | `"Slider"` | ARIA label for the thumb |

### Accessibility

- `role="slider"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax`
- Keyboard: ArrowRight/Up increments, ArrowLeft/Down decrements, Home/End jump to min/max
- 44px minimum touch target height

---

## Rating

Star rating component with keyboard and pointer support.

<Preview label="Rating">
  <RatingDemo />
</Preview>

```tsx
import { Rating } from "@weiui/react";

<Rating defaultValue={3} />
<Rating value={rating} onChange={setRating} />
<Rating allowHalf value={halfRating} onChange={setHalfRating} />
<Rating readOnly defaultValue={4} />
<Rating disabled defaultValue={2} />
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| max | `number` | `5` | Number of stars |
| value | `number` | — | Controlled value |
| defaultValue | `number` | `0` | Initial uncontrolled value |
| onChange | `(value: number) => void` | — | Called when a star is clicked |
| allowHalf | `boolean` | `false` | Clicking the left half of a star sets value to `N - 0.5`; ArrowRight/ArrowLeft step by `0.5` |
| disabled | `boolean` | `false` | Disables all interactions |
| readOnly | `boolean` | `false` | Prevents changes, still focusable |
| label | `string` | `"Rating"` | ARIA label for the group |

### Accessibility

- `role="radiogroup"` with individual stars as `role="radio"`
- `aria-checked` reflects selected star
- 44px minimum touch target height per star
- Roving tabIndex within the group

---

## InputNumber

Numeric input with increment/decrement buttons and keyboard step control.

<Preview label="InputNumber default">
  <InputNumberDemo />
</Preview>

<Preview label="InputNumber disabled">
  <InputNumberDisabledDemo />
</Preview>

```tsx
import { InputNumber } from "@weiui/react";

<InputNumber defaultValue={5} />
<InputNumber min={0} max={100} step={5} defaultValue={50} onChange={(v) => console.log(v)} />
<InputNumber disabled defaultValue={10} />
```

### Locale & currency formatting

Pass any `Intl.NumberFormatOptions` via the `formatOptions` prop to localise the display. The input still stores a plain number — formatting is applied only on render, and `aria-valuetext` mirrors the formatted string for screen readers.

<Preview label="InputNumber with formatOptions">
  <InputNumberCurrencyDemo />
</Preview>

```tsx
<InputNumber
  defaultValue={1234}
  formatOptions={{ style: "currency", currency: "USD" }}
/>

<InputNumber
  defaultValue={0.75}
  step={0.01}
  formatOptions={{ style: "percent", minimumFractionDigits: 2 }}
/>
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| min | `number` | `-Infinity` | Minimum allowed value |
| max | `number` | `Infinity` | Maximum allowed value |
| step | `number` | `1` | Step for increment/decrement |
| value | `number` | — | Controlled value |
| defaultValue | `number` | `0` | Initial uncontrolled value |
| onChange | `(value: number) => void` | — | Called on value change |
| disabled | `boolean` | `false` | Disables the control |
| formatOptions | `Intl.NumberFormatOptions` | — | Currency / percent / locale number formatting |
| locale | `string` | `"en-US"` | BCP-47 locale tag passed to `Intl.NumberFormat` |

### Accessibility

- Input is exposed as `role="spinbutton"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax`
- `aria-valuetext` announces the formatted value when `formatOptions` is set
- Increment/Decrement buttons have `aria-label`
- ArrowUp/ArrowDown keyboard support on the input
- Buttons auto-disable at min/max bounds
- 44px minimum height touch targets

---

## InputOTP

One-time password input with separate boxes per digit and auto-advance.

<Preview label="InputOTP 6-digit">
  <InputOTPDemo />
</Preview>

<Preview label="InputOTP 4-digit">
  <InputOTP4Demo />
</Preview>

```tsx
import { InputOTP } from "@weiui/react";

<InputOTP onChange={(value) => console.log(value)} />
<InputOTP length={4} onChange={(value) => console.log(value)} />
<InputOTP value={otp} onChange={setOtp} />
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| length | `number` | `6` | Number of digit slots |
| value | `string` | — | Controlled value |
| defaultValue | `string` | `""` | Initial uncontrolled value |
| onChange | `(value: string) => void` | — | Called when any slot changes |
| disabled | `boolean` | `false` | Disables all slots |

### Accessibility

- `role="group"` with `aria-label="One-time password"`
- Each slot has `aria-label="Digit N"`
- `inputMode="numeric"` for mobile numeric keyboard
- First slot sets `autoComplete="one-time-code"` so iOS/Android can surface the SMS code
- ArrowLeft / ArrowRight move focus between slots without clearing values
- Auto-advances focus to next slot on input
- Backspace clears current slot and moves focus back
- Paste distributes characters across slots

---

## AutoComplete

Text input with a filterable dropdown of suggestions. Anchored to the viewport via Floating UI so it can escape overflow clipping, and supports an async `loading` state.

<Preview
  label="AutoComplete"
  code={`import { AutoComplete } from "@weiui/react";

const options = [
  { value: "js", label: "JavaScript" },
  { value: "ts", label: "TypeScript" },
  { value: "py", label: "Python" },
];

<AutoComplete options={options} onChange={setValue} />`}
>
  <AutoCompleteDemo />
</Preview>

```tsx
import { AutoComplete } from "@weiui/react";

const options = [
  { value: "js", label: "JavaScript" },
  { value: "ts", label: "TypeScript" },
  { value: "py", label: "Python" },
];

<AutoComplete options={options} onChange={setValue} />

// Async loading state
<AutoComplete options={results} loading={isFetching} placeholder="Search packages..." />
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| options | `{ value: string; label: string }[]` | — | Suggestion list |
| value | `string` | — | Controlled selected option value |
| onChange | `(value: string) => void` | — | Fires when a suggestion is picked |
| onInputChange | `(input: string) => void` | — | Fires as the user types in the text box |
| placeholder | `string` | — | Input placeholder |
| emptyText | `string` | `"No results"` | Message shown when the filter has no matches |
| loading | `boolean` | `false` | Shows a loading row inside the dropdown |
| disabled | `boolean` | `false` | Disables the input |

### Accessibility

- Input exposed as `role="combobox"` with `aria-expanded`, `aria-controls`, `aria-activedescendant`
- Keyboard: ArrowDown opens the list, Arrow keys navigate, Enter selects, Escape closes
- Dropdown anchored with Floating UI — flips and shifts to stay on-screen

---

## MultiSelect

Pick multiple values from a list. The trigger shows selected chips and the dropdown ships with a built-in typeahead search input.

<Preview
  label="MultiSelect"
  code={`import { MultiSelect } from "@weiui/react";

const options = [
  { value: "frontend", label: "Frontend" },
  { value: "backend", label: "Backend" },
  { value: "devops", label: "DevOps" },
  { value: "design", label: "Design" },
];

<MultiSelect options={options} onChange={setValues} placeholder="Pick teams" />`}
>
  <MultiSelectDemo />
</Preview>

```tsx
import { MultiSelect } from "@weiui/react";

const options = [
  { value: "frontend", label: "Frontend" },
  { value: "backend", label: "Backend" },
  { value: "devops", label: "DevOps" },
  { value: "design", label: "Design" },
];

<MultiSelect options={options} onChange={setValues} placeholder="Pick teams" />
```

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| options | `{ value: string; label: string }[]` | — | Available options |
| value | `string[]` | — | Controlled selected values |
| defaultValue | `string[]` | `[]` | Initial uncontrolled values |
| onChange | `(values: string[]) => void` | — | Called when selection changes |
| placeholder | `string` | `"Select..."` | Text shown when nothing is selected |
| disabled | `boolean` | `false` | Disables the control |

### Accessibility

- Dropdown uses `role="listbox"` with `aria-multiselectable="true"`
- Built-in search input at the top of the dropdown filters options as you type
- Anchored via Floating UI for correct placement inside scrolling containers

---

## FileUpload

Drag-and-drop or click-to-browse file input with validation hooks.

<Preview label="FileUpload">
  <FileUploadDemo />
</Preview>

```tsx
import { FileUpload } from "@weiui/react";

<FileUpload
  accept="image/*"
  maxSize={5 * 1024 * 1024}
  onChange={(files) => console.log(files)}
  onError={({ reason, file, message }) => {
    toast.error(`${file.name}: ${message}`);
  }}
/>
```

### Error handling

`onError` fires when a drop or selection is rejected — e.g. wrong `accept` MIME type (`reason: "type"`), exceeded `maxSize` (`"size"`), or exceeded `maxFiles` (`"count"`). The handler receives `{ reason, file, message }` so you can pick whether to toast, inline-render, or silently log. An inline error is also rendered inside the dropzone automatically.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| accept | `string` | — | MIME types / extensions (same as `<input type="file">`) |
| multiple | `boolean` | `false` | Allow multiple files |
| maxSize | `number` | — | Max bytes per file |
| maxFiles | `number` | — | Max file count when `multiple` is true |
| onChange | `(files: File[]) => void` | — | Called when files are added or removed |
| onError | `(err: { reason: "type" \| "size" \| "count"; file: File; message: string }) => void` | — | Called when a file is rejected |
| label | `string` | `"Click or drag files here"` | Dropzone label text |
| hint | `string` | — | Optional helper text under the label |
| disabled | `boolean` | `false` | Disables drop + browse |

### Accessibility

- Dropzone acts as a button: `role="button"`, activates the hidden file input on Enter/Space
- Hint text renders inside the dropzone so screen readers pick it up via the `aria-label`
- Inline error region uses `role="alert"` for immediate announcement
