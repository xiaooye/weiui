import { ComponentPreview } from "../../../../components/ComponentPreview";

# Stepper & Timeline

Step progress indicators and chronological event displays.

---

## Stepper

Multi-step progress indicator showing active, completed, and pending steps.

<ComponentPreview label="Stepper horizontal">
  <div className="wui-stepper" style={{ display: "flex", gap: "var(--wui-spacing-4)" }}>
    <div className="wui-step" data-completed="">
      <div className="wui-step__indicator" style={{ width: "32px", height: "32px", borderRadius: "50%", background: "var(--wui-color-primary)", color: "var(--wui-color-primary-foreground)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "var(--wui-font-size-sm)" }}>&#10003;</div>
      <div>
        <div className="wui-step__label" style={{ fontWeight: "var(--wui-font-weight-medium)", fontSize: "var(--wui-font-size-sm)" }}>Account</div>
        <div className="wui-step__description" style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)" }}>Create your account</div>
      </div>
    </div>
    <div className="wui-step" data-active="" aria-current="step">
      <div className="wui-step__indicator" style={{ width: "32px", height: "32px", borderRadius: "50%", background: "var(--wui-color-primary)", color: "var(--wui-color-primary-foreground)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "var(--wui-font-size-sm)" }}>2</div>
      <div>
        <div className="wui-step__label" style={{ fontWeight: "var(--wui-font-weight-medium)", fontSize: "var(--wui-font-size-sm)" }}>Profile</div>
        <div className="wui-step__description" style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)" }}>Fill in details</div>
      </div>
    </div>
    <div className="wui-step">
      <div className="wui-step__indicator" style={{ width: "32px", height: "32px", borderRadius: "50%", border: "1px solid var(--wui-color-border)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "var(--wui-font-size-sm)", color: "var(--wui-color-muted-foreground)" }}>3</div>
      <div>
        <div className="wui-step__label" style={{ fontWeight: "var(--wui-font-weight-medium)", fontSize: "var(--wui-font-size-sm)" }}>Review</div>
        <div className="wui-step__description" style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)" }}>Confirm setup</div>
      </div>
    </div>
  </div>
</ComponentPreview>

```tsx
import { Stepper, Step, StepSeparator } from "@weiui/react";

// Explicit indices
<Stepper activeStep={1}>
  <Step index={0} label="Account" description="Create your account" />
  <StepSeparator index={0} />
  <Step index={1} label="Profile" description="Fill in details" />
  <StepSeparator index={1} />
  <Step index={2} label="Review" description="Confirm setup" />
</Stepper>

// Auto-inferred indices — Stepper walks its children and assigns an
// index to each <Step>; separators take the preceding step's index.
<Stepper activeStep={1}>
  <Step label="Account" description="Create your account" />
  <StepSeparator />
  <Step label="Profile" description="Fill in details" />
  <StepSeparator />
  <Step label="Review" description="Confirm setup" />
</Stepper>
```

You can mix the two: an explicit `index` on any child overrides the auto-assigned value.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| activeStep | `number` | — | Current active step index |
| orientation | `"horizontal" \| "vertical"` | `"horizontal"` | Layout direction |

### Step / StepSeparator Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| index | `number` | auto | Explicit step index. Omit to let `Stepper` auto-infer from child order. |
| label | `ReactNode` | — | Step title (Step only) |
| description | `ReactNode` | — | Step supporting text (Step only) |

---

## Timeline

Chronological event display with dots and connecting lines.

<ComponentPreview label="Timeline">
  <ol className="wui-timeline" style={{ listStyle: "none", padding: 0, display: "flex", flexDirection: "column", gap: "var(--wui-spacing-4)" }}>
    <li className="wui-timeline-item" style={{ display: "flex", gap: "var(--wui-spacing-3)" }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
        <div style={{ width: "12px", height: "12px", borderRadius: "50%", background: "var(--wui-color-primary)" }} />
        <div style={{ flex: 1, width: "2px", background: "var(--wui-color-border)" }} aria-hidden="true" />
      </div>
      <div>
        <div style={{ fontWeight: "var(--wui-font-weight-medium)", fontSize: "var(--wui-font-size-sm)" }}>Application submitted</div>
        <div style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)" }}>Your application was received</div>
        <div style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)", marginTop: "var(--wui-spacing-1)" }}>Jan 15, 2026</div>
      </div>
    </li>
    <li className="wui-timeline-item" style={{ display: "flex", gap: "var(--wui-spacing-3)" }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
        <div style={{ width: "12px", height: "12px", borderRadius: "50%", background: "var(--wui-color-primary)" }} />
      </div>
      <div>
        <div style={{ fontWeight: "var(--wui-font-weight-medium)", fontSize: "var(--wui-font-size-sm)" }}>Under review</div>
        <div style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)" }}>Currently being reviewed by our team</div>
        <div style={{ fontSize: "var(--wui-font-size-xs)", color: "var(--wui-color-muted-foreground)", marginTop: "var(--wui-spacing-1)" }}>Jan 20, 2026</div>
      </div>
    </li>
  </ol>
</ComponentPreview>

```tsx
import { Timeline, TimelineItem } from "@weiui/react";

<Timeline>
  <TimelineItem title="Application submitted" description="Your application was received" time="Jan 15" />
  <TimelineItem title="Under review" description="Being reviewed by our team" time="Jan 20" />
</Timeline>
```

### Accessibility

- Uses semantic `<ol>` and `<li>` elements
- Connecting lines are `aria-hidden="true"`
- Server component — no `"use client"` required
