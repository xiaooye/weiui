// @deprecated — use ./compute-drop-indicators.
// This shim exists for one release to keep import-path compatibility.
export {
  computeDropAction,
  locateNode,
  isSelfOrDescendant,
  isInSubtree,
  type DropContext,
  type Edge,
} from "./compute-drop-indicators";
