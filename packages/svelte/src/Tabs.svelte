<script lang="ts">import {createTabsController} from "@civaria/core";import {normalizeSvelteProps} from "./internal.js";type Item={value:string;label?:string;content?:string;disabled?:boolean};let {items=[],value=$bindable(""),orientation="horizontal",id="civ-tabs"}=$props<{items?:Item[];value?:string;orientation?:"horizontal"|"vertical";id?:string}>();const controller=createTabsController({id,defaultValue:value,orientation,onValueChange:next=>value=next});let state=$state(controller.getState());$effect(()=>{const off=controller.subscribe(next=>state=next);return off});$effect(()=>{if(value!==state.value)controller.syncValue(value)});function p(kind:"root"|"list"|"trigger"|"content",item?:Item){state;return normalizeSvelteProps(kind==="root"?controller.getRootProps():kind==="list"?controller.getListProps():kind==="trigger"?controller.getTriggerProps(item!.value,item!.disabled):controller.getContentProps(item!.value))}</script><div {...p("root")} class="civ-tabs"><div {...p("list")} class="civ-tabs__list">{#each items as item (item.value)}<button {...p("trigger",item)} class="civ-tabs__trigger">{item.label??item.value}</button>{/each}</div>{#each items as item (item.value)}<div {...p("content",item)} class="civ-tabs__content">{item.content??""}</div>{/each}</div>
