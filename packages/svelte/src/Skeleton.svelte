<script lang="ts">import { semanticPart } from "@civaria/core"; let { variant, size, class: className, ...rest } = $props<{ variant?: string; size?: string; class?: string; [key:string]:unknown }>(); let attrs=$derived({ ...rest, ...semanticPart("skeleton","root",{variant,size}) });</script><div {...attrs} class="civ-skeleton {className ?? ''}"></div>
