<script lang="ts">import type { Snippet } from "svelte"; import { semanticPart } from "@civaria/core"; let { children, variant, size, class: className, ...rest } = $props<{ children?:Snippet; variant?: string; size?: string; class?: string; [key:string]:unknown }>(); let attrs=$derived({ ...rest, ...semanticPart("grid","root",{variant,size}) });</script><div {...attrs} class="civ-grid {className ?? ''}">{@render children?.()}</div>
