<script lang="ts">import type { Snippet } from "svelte"; import { semanticPart } from "@civaria/core"; let { children, variant, size, class: className, ...rest } = $props<{ children?: Snippet; variant?: string; size?: string; class?: string; [key:string]:unknown }>(); let attrs=$derived({ ...rest, ...semanticPart("badge","root",{variant,size}) });</script><span {...attrs} class="civ-badge {className ?? ''}">{@render children?.()}</span>
