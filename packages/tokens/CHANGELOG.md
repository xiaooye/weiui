# @civaria/tokens

## 0.1.0

### Minor Changes

- d84d1cd: Add a canonical `civ-theme` cascade slot for product-level semantic token overrides and make the framework-agnostic CSS consumption path explicit for non-React hosts.

## 0.0.1

- Initial pre-release: W3C Design Tokens Community Group JSON as source-of-truth. Emits `tokens.css` (custom properties), `tokens.json`, and TypeScript. Validation enforces WCAG AAA contrast (7:1 normal / 4.5:1 large) using `@civaria/a11y`.
