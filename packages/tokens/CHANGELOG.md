# @weiui/tokens

## 0.0.1

- Initial pre-release: W3C Design Tokens Community Group JSON as source-of-truth. Emits `tokens.css` (custom properties), `tokens.json`, and TypeScript. Validation enforces WCAG AAA contrast (7:1 normal / 4.5:1 large) using `@weiui/a11y`.
