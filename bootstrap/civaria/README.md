# Civaria

**WCAG-first. Governance-ready.**

This is the bootstrap release that reserves the `civaria` package name while the design-system repository completes its full product and package rename.

The production entrypoint will replace this bootstrap package in a subsequent release.
